# Frozen Mathematical Statements

## M1 — Geometry

- Unit-radius `Vol(S3)=2*pi^2` and `Vol(RP3)=pi^2`.
- Unit systole `pi`.
- `S_geo=4*pi^3+pi^2+pi`.
- Minimal-class uniqueness is conditional, not a universal theorem.

## M2 — Spectra

- On the `S3` cover, Dirac eigenvalues are `+/- (n+3/2)/R3`, with multiplicity `(n+1)(n+2)` for each sign. The `RP3` spectrum must be obtained by a separate spin-structure-dependent `Z2` projection.
- Scalar modes on `L(2,1)` retain even angular degree, with multiplicity `(ell+1)^2` in the declared untwisted branch.
- Coexact one-form eigenvalues and multiplicities must be reconstructed representation-theoretically, including the declared parity projection.

## M3 — Zeta and Heat Kernel

- `zeta(4,1/2)=pi^4/6`.
- Periodic `1/24` is conditional on the declared finite-part and determinant convention.
- `Var(x_bar)=1/(6*zeta(4,1/2))=pi^-4` is an algebraic Gaussian identity only.
- C6 absorption is reproduced as a negative result.

## M4 — Gram and Projectors

- `Q_cycle=diag(pi,pi^-1)` and `(1,1) Q_cycle (1,1)^T=pi+pi^-1`.
- `23+pi^-1` is a counterfactual identity conditional on a rank-one projector.
- The generation-covariant identity is `21+3/pi` for a rank-three kernel under the ordinary trace.
- Neither identity is an active physical neutrino prediction.

## M5 — B-L Audit

- Recompute all six local anomaly sums with one sterile conjugate per generation.
- Verify the root holonomy and charge conservation of the Majorana vertex.
- Reproduce the root--mass--condensate trilemma and the GL threshold.

## M6 — No-Go Map

Reproduce the family-selector, phase-to-mass, conformal-rank, determinant-sign, threshold-direction, two-sector normalization, and unified-parent-action negative gates from their stated assumptions.
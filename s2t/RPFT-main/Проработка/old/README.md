# Архив

Эта папка содержит исторические снимки материалов из соседней рабочей папки `Проработка`.

Файлы здесь:

- не считаются независимыми источниками;
- не считаются повторными вычислительными подтверждениями;
- не включаются в число действующих исследовательских результатов;
- сохраняются только для проверки истории изменений.

Точные копии `g-2.md`, `sp2.md`, `standart_model.md`, `standart_model2.md` и `w-bazon.md` намеренно оставлены в архиве.
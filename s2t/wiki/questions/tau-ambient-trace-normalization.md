# Tau Ambient Trace Normalization

> Status: rank-nine half-trace candidate rejected
> Date: 2026-08-04

## Quotient Trace

The traceless quadratic strain sector is even under the antipodal action. Therefore

```text
P_plus=(I+U)/2=I
```

on this rank-nine space, and `Tr(P_plus)=9`, not `9/2`. The second half of the group average is the image contribution and cannot be discarded.

## Mode Normalization

A normalized constant mode on `RP3` is `sqrt(2)` times the corresponding normalized invariant mode on `S3`. Its squared amplitude cancels the quotient's half-volume in a quadratic matrix element. Thus normalized quotient modes do not supply an extra factor `1/2`.

For a single collective rank-nine deformation, canonical Hilbert--Schmidt normalization gives

```text
P_hat=P_tr/sqrt(Tr P_tr^2)=I_9/3,
Tr(P_hat^2)=1.
```

Nine independently normalized fields give trace `9`. Neither convention gives `9/2`.

## Consequence

The canonical single-mode coefficient predicts

```text
m_tau=1777.06887 MeV  (+1.54 sigma).
```

It remains compatible with current data, but does not reproduce the proposed `alpha/3` normalization. The background Gram seed also uses unnormalized geometry modes; normalized charged-lepton wavefunctions would not retain the volume factors `pi^2` and `2pi`.

## Verdict

The `J=9/2` completion is closed negatively in the minimal normalized ambient model. Reopening requires an independently derived noncanonical stiffness or measure factor, not another choice based on the tau target.
# Version IV family-defect gauge-family locking gate

## Question

Can the tetrahedral gauge flux acquire a physical family orientation, and does the same locking mechanism force the ordinary charge-two `B-L` pairing condensate?

## Locking construction

- Introduce a real bifundamental `X -> g X f^-1` under gauged `SO(3)` and global family `SO(3)`.
- Use `V_lock=1/4||X^T X-I||^2+1/4(det X-1)^2`.
- Its vacuum locks the two groups to the exact global diagonal `SO(3)_(g+f)`.
- The Hessian has signature `(6+,3zero,0-)`; the three gauge-orbit modes are eaten and all three gauge masses are positive.
- The tetrahedral tensor then reduces the diagonal group to `A4`, and the projector leaves `Z3`.

## Pairing result

The most general minimal portal gives `m_eff^2=m_Phi^2+3 kappa`. The same symmetry permits both `m_eff^2>0` and `m_eff^2<0`, so diagonal locking does not select condensation.

The coefficient-free candidate

`V_mu=(|Phi|^2-Tr(X^T X)/3)^2`

forces `|Phi|=1` in the locked vacuum, but the coefficient ratio `1:-2/3:1/9` must still be derived from one central curvature, D-term, or projected superconnection moment map.

## Status

The gauge/global axis fork passes. The remaining decisive gate is a finite graded supertrace derivation of the norm-locking square or an equivalent negative pairing Hessian without a fitted portal coefficient.

## Files

- `version4_family_defect_gauge_family_locking_gate.tex`
- `s2t_v4_family_defect_gauge_family_locking_gate.py`
- `s2t_v4_family_defect_gauge_family_locking_gate_results.json`
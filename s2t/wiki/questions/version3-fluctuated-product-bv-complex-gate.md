# Version III Fluctuated Product and BV Complex Gate

> Status: local product lift constructed
> Date: 2026-08-10

## Construction

The finite KO6 module is lifted to the almost-commutative product:

    A = Cinf(K) tensor (C plus C)
    H = L2(K,S) tensor H8
    D = D_K tensor 1 + gamma_K tensor chi D_F*

The bimodule charge vector is (0,+1,-1,0). The diagonal U(1) acts
trivially, leaving a unique faithful relative U(1).

## Gauge and BV Result

The finite parameters carry opposite unit charges, so xz and the
flattening potential remain gauge invariant. At the equal-modulus vacuum,
the trace kinetic term yields m_A^2/chi^2 = 8 g^2.

The vector, Goldstone and complex ghost blocks produce 4+1-2=3 physical
massive-vector degrees of freedom and the zero-mode numerator 192 g^4.

## Milestone

The physical gauge quotient, charge unit and minimal BV Hessian complex are
no longer free choices. The former arbitrary gauge mass ratio has been
reduced to the single canonical coupling g.

## Remaining Gate

Derive the gauge kinetic coefficient from the same heat-kernel a4 term
that fixed the scalar normalization. This must determine whether the common
spectral moment fixes g or remains a continuous input before the
spin/flat KK audit begins.
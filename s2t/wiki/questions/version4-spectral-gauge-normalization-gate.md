# Version IV.SM Spectral Gauge Normalization Gate

## Trace Result

For one generation,

- `C_Y=10/3`;
- `C_2=2`;
- `C_3=2`.

With GUT-normalized hypercharge, `C_1=(3/5)C_Y=2`, so a common spectral
coefficient gives `g1=g2=g3`, equivalently `gY^2=(3/5)g2^2` and
`sin^2(theta_W)=3/8` at the matching scale.

## One-Loop RG Test

Using the frozen MZ inputs, the pairwise crossings are approximately

- `mu12=1.03e13 GeV`;
- `mu13=2.43e14 GeV`;
- `mu23=9.73e16 GeV`.

There is no common triple crossing. At `g1=g3`, `g2` is about 4.24 percent
lower. Experimental input uncertainties do not close the gap.

## Verdict

The spectral trace ratio is exact, but minimal no-threshold one-loop Standard
Model running fails exact spectral unification. Any threshold repair must be
derived from new representations rather than fitted independently.
# Version III Dimensional Product Consistency Gate

> Status: architecture choice required before KK summation
> Date: 2026-08-10

## Conflict

Tome II describes K=RP3 x S1 as an internal compact carrier. The recent
local product lift used Cinf(K) as the four-dimensional spacetime algebra.
These interpretations cannot be combined in one Coleman--Weinberg ledger.

## Base-K Branch

If K is the Euclidean spacetime base, its eigenmodes are determinant modes,
not additional particle species. Adding a separate KK sum would double
count the same field modes.

## Internal-K Branch

If K is internal, the local geometry is M4 x K and is eight-dimensional.
The heat-kernel coefficients factorize and the flat external a4 block is
only a zero-mode truncation.

A simple spinorial internal lift also has no fermion zero modes. The round
product has scalar curvature R=6, so the Lichnerowicz bound is R/4=3/2 and
the geometric internal Dirac operator has no kernel.

## Corrected Status

The results kappa=2, g^2=3/8 and B_zero=67/(64 pi^2) remain valid for the
four-dimensional zero-mode effective parent action. They are not yet a
derived M4 x K compactification.

## Next Gate

Choose either a full determinant directly on base K, or construct an
internal operator, twist and projector that genuinely preserve the required
four-dimensional zero modes before any KK correction is computed.
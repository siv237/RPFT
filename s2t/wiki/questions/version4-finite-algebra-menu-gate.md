# Version IV.SM Finite Algebra Menu Gate

## Result

- The declared block menu was exhausted through three summands.
- `C + H + M3(C)` is the unique real-dimension minimum inside the
  quaternionic weak-block branch with one surviving central U(1).
- Before unimodularity the Lie algebra is
  `u(1) + su(2) + su(3) + u(1)`.
- One nondegenerate central relation leaves
  `su(3) + su(2) + u(1)`.
- The target one-generation bimodule has 16 complex particle states before
  antiparticle doubling.

## Limitation

This is a constrained baseline theorem. The bimodule ledger is still
target-loaded; the discrete quotient, three generations and Yukawa matrices
remain open.
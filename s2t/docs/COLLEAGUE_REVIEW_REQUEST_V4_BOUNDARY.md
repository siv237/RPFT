# Запрос коллеге: red-team минимального boundary connector

Передаю актуальный Том IV и прошу проверить следующую новую ветвь.

## Контекст

Уже зафиксированы:

- observed algebra `C + H + M3(C)`;
- один 16-state generation atom;
- conditional affine family triplet;
- rank-one leading texture;
- отсутствие selector-а в quadratic incidence menu;
- CP no-go real и linear orientation-odd menus;
- exact gauge trace relation и one-loop no-threshold mismatch;
- один Higgs doublet и четыре Yukawa edge types;
- exact decoupling hidden/observed direct sum.

## Новый ansatz

Restricted capability scan показывает, что один scalar, одна vectorlike pair
или одна messenger chain не могут одновременно дать portal, family breaking
и физическую CP phase. Первый формально способный package — две vectorlike
messenger paths плюс family-triplet connector, образующие oriented closed
Krajewski loop.

## Вопросы для проверки

1. Можно ли реализовать такой two-path loop в real even finite geometry,
   сохранив grading, `J` и order-one condition?
2. Каков минимальный набор SM representations и hidden `U(1)` charges для
   messenger orbits, чтобы все локальные и возможные discrete anomalies
   сокращались?
3. Может ли один off-diagonal finite Dirac block породить одновременно
   `Sigma_h H^dagger H`, family-triplet breaking и orientation-odd invariant,
   или для этого неизбежны независимые coefficients?
4. Как доказать, что phase произведения edges по closed loop переживает все
   допустимые field rephasings и real-structure identifications?
5. После интегрирования messengers возникает ли фиксированный rank-one plus
   incidence Yukawa operator, или возвращается произвольная `M3(C)` matrix?
6. Есть ли более минимальная архитектура, пропущенная restricted menu, которая
   проходит те же пять capability requirements?

Нужен максимально жёсткий ответ: первая математическая ошибка, скрытые
нормировки, anomaly obstruction и минимальный исправимый вариант.
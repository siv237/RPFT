# Version III RG and Anomaly Scale-Setting Gate

> Status: one absolute scale input is unavoidable
> Date: 2026-08-10

## Gauge Running

The physical charge ledger gives sum q_f^2=2 for Weyl fermions and
sum q_s^2=2 for the two complex scalars. The one-loop abelian coefficient
is therefore b=2.

There is no nonzero perturbative one-loop fixed point. The spectral result
g^2=3/8 must be read as a matching condition at a scale mu_spec.

## Dimensionless Prediction

The matching condition fixes

    log(Lambda_L/mu_spec) = 32 pi^2/3.

This is a dimensionless ratio, not an absolute scale prediction.

## General No-Go

Dimensional transmutation creates an RG integration constant. Trace anomaly
describes scale breaking but does not set the boundary value of the RG
trajectory. One mass or length input remains unavoidable.

## Next Gate

Choose one preregistered scale-setting input and reserve at least two
independent dimensionless observables for a blind two-sector scorecard.
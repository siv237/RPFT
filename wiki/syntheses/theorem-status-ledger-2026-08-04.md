# Theorem Status Ledger — 2026-08-04

> Status: working
> Research status: active decision ledger
> Type: synthesis
> Updated: 2026-08-15

> Rule: a numerical match is not a theorem unless its operator, measure, normalization and blind inputs are fixed before comparison.

Revision R1 has passed a separate direct numerical reproduction and therefore
reaches internal maturity R_sci=5/10 under the predeclared rubric. This
changes documentation and reproducibility status only. It does not change
the theorem ledger or N_closed_physical=0. See
[[revision-r1-route-triage-2026-08-07]].

The K1--K14 batch adds no theorem. Doubled eta reaches 1/3 only as phase
arithmetic; six reversed BV zero shells reach pi^4 only by an underived
multiplicity; and the exact inverse susceptibility still lacks a carrier and
source map. The defect rank-one kernel remains the strongest conditional
operator result. See [[k1-k14-loop-reverse-audit-2026-08-07]].

The complete two-form BV follow-up confirms the tensor verdict. One reversed
complex yields only +pi^4/6 on the spatial zero shell, the full quotient has
one physical mode, no positive commuting variance follows from the Grassmann
measure, and neither J^2/2=1 nor a second sector is derived. The status is
algebraic identity pass / parent-action fail.

The project-level interpretation of the current ledger is summarized in
[[kinematics-dynamics-pairing-diagnosis-2026-08-07]]. The recurrent success of
paired structures is classified there as an architectural heuristic. The
ledger does not prove that pairing is necessary or sufficient, that all local
four-dimensional parent actions fail, or that every open sector is the image
of one unique missing weight principle.

## Closed Positive Results

- The carrier bookkeeping for `K=RP3 x S1`, quotient volume, spin cover and the nontrivial `Z2` flat line is internally consistent.
- The untwisted `RP3` scalar and coexact multiplicities reproduce the external lens-space spectrum, and the published scalar determinant is independently recovered.
- The systolic cycle Gram operator is `Qcycle=diag(pi,pi^-1)` and its primitive self-dual vector has squared norm `pi+pi^-1`.
- Inside the explicitly declared square-root-torsion Majorana defect model, local core gluing leaves one real periodic zero line.
- Given the canonical graded trace--Hodge metric, the parent tubular restriction has norm `23+pi^-1` with no relative continuous coefficient.

## Closed Negative Results

- Standard Maxwell--FP bookkeeping does not derive the exact `pi^-4` absorption term. It remains structural compression.
- The intrinsic cycle duality does not lift to an ambient `r <-> 1/r` Maxwell--FP symmetry. The symmetrized unit-radius minimum is not vacuum selection.
- Exact current symmetries do not select an invariant real rank-one spinor line; the defect is additional structure.
- A rank-23 heavy quotient does not by itself produce a mass denominator `23`.
- Generic positive heat-kernel spectral actions do not give equal heavy and kernel Hessian weights at nonzero background.
- The minimal unified trace--Hodge parent action fails the two-sector gate: it preserves `23+pi^-1` but canonical normalization removes the raw tau volume seed and does not generate `1/3`.
- The global residual-claim audit finds zero closed independent empirical predictions in II.A. The current version is rejected as a unified predictive physical theory.

## Conditional Results

- The minimal `B-L` extension with one `N_c` per generation passes all six
  continuous anomaly sums and supplies the sterile `+i` root and invariant
  charge-two pairing vertex. Its only surviving minimal realization is a
  nonuniform defect texture conditional on `lambda v^2>1`; topology fixes the
  half-shift, conjugate pair and unit winding magnitude, but not condensation,
  orientation, `v_(B-L)` or kinetic mixing. See
  [[bl-nonuniform-pairing-working-package-2026-08-07]].
- A later explicit B-L parent-action proposal fails global consistency. The
  torsion twist allows a parallel condensate but makes the Majorana vertex
  torsion-odd; total holonomy +1 also removes the claimed half shift. The
  ordinary nonuniform defect branch remains the only survivor. See
  [[bl-defect-action-global-consistency-gate]].
- The exact six-channel identity `Var(xbar)=pi^-4` and the exact `Z2/Z4`
  projection direction `(17/6,1/6,2)` are independent constructions. They do
  not form a unified physical result without a common operator and measure.
- The ten-hypothesis rerun in [[hypothesis-batch-pruner-gate]] adds no closed
  physical result. Its new strict filters are circle-determinant evenness,
  circularity of the solved two-loop coefficient, and the nonexactness of
  `12 pi/7` as the required projection weight.
- The tiered parent-action proposal passes the numerical norm identities but
  fails its first operator gate. Constant tau zero-forms have no quantized
  period, so one deterministic type rule yields either the raw seed or the
  canonical `8/3` control, not both. See [[tiered-parent-action-p1-gate]].
- The explicit `W`-map audit rejects constant and degree-only rules. A
  factor-plus-degree table reproduces the tau and neutrino norms, but has no
  parent-action derivation or field-redefinition covariance. It is therefore
  a lookup pass, not a two-sector physical closure. See [[weight-map-gate]].
- H-M consistently uses the frozen canonical graded metric to obtain
  23+pi^-1 and the tau normalization control 8/3, but this does not derive
  the metric or add an independent empirical sector. Moving the raw tau seed
  to a vertex localizes the gap; it does not derive the seed or
  J_req=5.40275331. Only the specific Q_cycle vertex fails, while a general
  single-vertex no-go remains unproved. See
  [[canonical-measure-vertex-localization-gate]].
- `S_geo`, the charged-lepton relation and the Higgs EFT bridge remain model results whose assumptions must stay explicit.
- The tau formula is unique inside the frozen low-complexity grammar, but `rho0` is postulated and the displayed compact loop requires an uncomputed projection Jacobian to produce `1/3`.
- A Gram tangent realizes the raw background seed, but canonical mode normalization does not preserve its volume norms automatically.
- The rank-nine half-trace candidate `J=9/2` is closed negatively: the even quotient trace is `9`, while a canonical single collective deformation gives `J=1`.
- The Higgs potential and `lambda_H` normalization remain structural results; absolute `v_S2T` and `M_H` inherit the conditional tau and `S_vac` scale.
- The Higgs potential yields `v_S2T` internally, but its tree-level identification with the physical weak scale fails the blind `G_F` test by `0.184%`; finite electroweak matching is open.
- The absolute neutrino scale is a theorem only inside the model that takes the canonical affine configuration metric as primary and includes the square-root-torsion defect.
- EW/QCD currently supplies high-scale trace normalizations, not a blind low-energy closure.
- The frozen scorecard shows a close charged-lepton/scalar cluster but a jointly failed gauge-running cluster; the dominant missing object is representation-dependent low-energy matching.
- Complete-generation KK replicas fail the required threshold direction; a split singlet-heavy tower is necessary but has not been derived.
- An anomaly-free vectorlike parent and Z2/Z4 projection for the split tower have been constructed; the character origin and finite threshold magnitude remain conditional.
- The common-spectrum determinant of shifted partners fails the required gauge direction exactly.
- Correct-sign one- and two-loop running closes the periodic `U+2D+H` intermediate-scale repair negatively.
- The corrected `XY/H3/Sigma8/Sigma3` finite-threshold cone is physically infeasible within the full `M_Z...Lambda_S2T` interval.
- The zero-parameter action `pi^2+1/(2pi)` defines a conditional fixed-background saddle, but its claimed gauge improvement is invalid because the legacy correction had the wrong sign and violated the `alpha_em` anchor.
- On a frozen unit carrier, that action is the unique stable minimum of a degree-one wrapping plus unit-period cycle functional; it fails to make the unit radius stationary when scale variations are admitted.

## Open Decisions

1. **Primary-metric decision.** Either declare the canonical configuration metric as an S2T kinetic postulate, or keep the neutrino denominator conditional. A generic spectral cutoff cannot supply it.
2. **Independent blind gate.** Derive finite KK/breaking-sector matching with the required sign; ordinary split running has already failed.
3. **New-theory gate.** A winding or T-dual ambient sector may be studied only if derived independently of the failed `pi^-4` and radius targets.
4. **Split-action gate.** Derive the wrapping tension, cycle kinetic normalization and radius stabilization from one parent functional before treating the intermediate mass as action-level evidence.
5. **Gauge stop rule.** Reopen EW/QCD only for an independently derived nonlogarithmic matching operator or UV normalization; additional fitted threshold masses are not admissible.
6. **Tau normalization gate.** Derive `rho0` as a charged-lepton operator eigenvalue and compute the normalized `RP3` projection trace before promoting tau or the inherited Higgs scale.
7. **Charged-lepton action gate.** Reopen normalization only for an independently derived noncanonical stiffness or measure operator; the minimal ambient trace has already selected `J=1`.
8. **Unified-action gate.** Do not combine sector formulas into a physical theory until one prior action reproduces at least two independent normalization-sensitive sectors without added weights.

## Stop Rules

- Do not reopen the homogeneous `B-L` condensate or full torsion-rescue routes;
  the root--mass--condensate trilemma has closed them.
- Do not reopen a closed no-go by changing determinant schemes after seeing the target.
- Do not promote a kernel selected at one physical mass to a universal derivation.
- Require at least two blind observables from every new continuous parameter or new sector.
- If a branch produces only the quantity used to design it, classify it as a model completion, not evidence for the common source.

## Current Recommendation

Terminate phenomenological development of II.A. Preserve its mathematical results and no-go theorems. Reopen physical work only as a preregistered new version with one prior action, one fixed measure and at least two independent observables.
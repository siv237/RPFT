# Определения

Кандидат описывается четвёркой: целый вес длины `weight`, булевы признаки
`internal`, `typedMap`, `breaksOrbit`. Он проходит тогда и только тогда,
когда `weight=-2` и все три признака истинны.
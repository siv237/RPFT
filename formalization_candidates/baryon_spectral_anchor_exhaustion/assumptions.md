# Предпосылки

- Реестр содержит ровно шесть явно перечисленных классов текущего проекта.
- Вес массы в квадрате равен `-2` относительно длины.
- Внешняя наблюдаемая не считается внутренне выбранной.
- Формализация доказывает исчерпание реестра, а не всех мыслимых теорий.
# Definitions

- `iota: K43 -> K45` is the canonical inclusion of the old cell.
- `Iota=otimes_m iota_m` is the induced vacuum-reference chain embedding.
- `Q_new=I45-iota iota^*` projects onto the charged Real pair.
- Structural admission means exact preservation under restriction, not
  physical selection of coefficients.
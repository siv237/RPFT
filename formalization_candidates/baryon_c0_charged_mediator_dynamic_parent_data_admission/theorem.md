# Theorem

> Status: spec-frozen

The canonical cell inclusion `K43 -> K45` preserves the reference vacuum,
compresses the new local parent to the old one, intertwines the chain
shifts, and recovers the old Floquet process when the new coupling is zero.
Thus the dynamic extension is structurally conservative, while all five
physical parent selectors remain absent.
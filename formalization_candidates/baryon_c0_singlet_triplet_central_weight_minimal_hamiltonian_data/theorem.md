# Минимальность безразмерной центральной щели
> Status: spec-frozen

Доказать биекцию `theta in R <-> p in (0,1)`, строгую выпуклость
Gibbs-функционала и невозможность раздельно восстановить `beta` и `Delta`
из одного равновесного веса.
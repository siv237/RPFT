# План формализации и проверки

1. Классифицировать коммутант гамильтониана представления `1+3`.
2. Факторизовать общий сдвиг энергии.
3. Доказать биекцию и монотонность `theta -> p`.
4. Проверить Gibbs-вариационное тождество через относительную энтропию.
5. Выделить масштабную орбиту `(beta,Delta)` и отдельный слой скорости.
# Definitions

- The selector packages are endpoint-score order, energy quartic ratio,
  coupling quartic ratio, and transport-bias sign.
- Closure violations count missing mandatory endpoint structures.
- Conditional endpoint selection is distinct from raw endpoint origin.
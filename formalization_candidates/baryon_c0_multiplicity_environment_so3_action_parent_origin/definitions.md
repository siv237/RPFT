# Определения

`W_cur=span(E_00,E_11,E_21)` — текущее вещественное пространство
коннекторов внутри `Hom(R2,R3)`. `SO(3)` действует слева стандартной
тройкой; двумерный source имеет тривиальное действие.
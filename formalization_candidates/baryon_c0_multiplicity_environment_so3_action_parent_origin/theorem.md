# Удвоение при минимальном семейном замыкании
> Status: spec-frozen

Доказать, что `W_cur` не `SO(3)`-инвариантно, его минимальное замыкание
равно `Hom(R2,R3)=3+3`, а пространство интертвейнеров из семейной тройки
двумерно и после изометрии оставляет `RP1`.
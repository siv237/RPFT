# Definitions

- The four slots are endpoint sector, common energy `E_star`, dimensionless
  coupling `chi`, and transport-law class.
- Program admission means that every slot and every success condition has a
  computable type and pass/fail test.
- Construction is stronger than admission and remains a separate theorem.
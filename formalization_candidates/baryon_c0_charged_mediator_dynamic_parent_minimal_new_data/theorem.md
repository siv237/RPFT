# Theorem

> Status: spec-frozen

After `E_star=E_C=7 gamma`, all scalar dynamic quantities are parametrized
by exactly two continuous coordinates `(E_star,chi)`. Together with the
endpoint extension and transport primitive, the minimal new-data package
has four independent slots. None is selected by the current parent.
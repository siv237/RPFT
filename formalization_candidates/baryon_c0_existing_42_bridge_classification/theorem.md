# Старый кадр не содержит внешнего imprimitivity bridge
> Status: spec-frozen

Доказать, что каждый нулём продолженный элемент старого span аннулирует
`Paux`, поэтому не удовлетворяет `E*E=Paux`. Отдельно доказать нулевую
кратность нейтрального irrep и тройную кратность charged singlet `Y=-1`.
# Theorem

> Status: spec-frozen

The three endpoint phases admit a faithful configuration carrier of complex
dimension `68` with phase algebra `C^3` and a self-adjoint block Dirac of
rank `46`. Its canonical inclusion edges preserve a reducing old subbundle
of dimension `63` and create none of the three new physical endpoint lines.
# Test plan

1. Verify the predecessor names this gate.
2. Reconstruct the exact `1-6-3` exterior-chain matrix over six rational variables.
3. Solve the tracelessness equation for the middle weight.
4. Compute `Tr(Qhat D_B²)` and moments `Tr X^k`, `k=2,3,4`.
5. Verify the fixed portal/self-cubic ratio and the source at `T=3`.
6. Compute the commutant dimensions of the channel `1+2` and family `3` representations.
7. Reject every SymPy `Float`.

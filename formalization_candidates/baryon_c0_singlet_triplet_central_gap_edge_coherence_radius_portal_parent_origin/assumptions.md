# Assumptions

- The exact exterior-chain definitions of `A_B` and `C_B` from Tome VII are retained.
- The singlet and triplet endpoint eigenvalues of the central direction are `-3/4` and `1/4`.
- The inherited spectral parent contains only its registered even moments.
- A cubic coefficient is not admitted merely because its invariant is algebraically nonzero.

# Тепловая параметризация без происхождения центрального веса
> Status: spec-frozen

Доказать, что каждый `0<p<1` единственным образом представим как
`beta Delta=log(3p/(1-p))`, тогда как обычный след и максимум энтропии
дают `p=1/4` лишь после дополнительного селекторного принципа.
# Definitions

- `L_down=|d><u|`, `b_+=|+><0|`.
- `Y_E=diag(0,1)` and `H_E=diag(0,7 gamma)`.
- `G=g(L_down tensor b_+ + L_up tensor b_+^*)`.
- `Phi_theta` is the reduced channel with the environment initialized in
  `|0><0|`.
- The Real completion has environment charges `(0,+1,-1)`.
# Definitions

- `E_min` is the three-line system-endpoint module containing `s0+`, `a0-`
  and `eR_t0+`.
- Raw origin requires simultaneous agreement of gauge, grading, family, Real
  and tensor-factor types.
- Environment retyping is forbidden when it destroys the system/environment
  factorization.
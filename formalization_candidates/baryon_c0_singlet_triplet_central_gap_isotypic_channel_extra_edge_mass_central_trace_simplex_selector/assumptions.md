# Assumptions

- The mass map is `(2p_Y,3p_u,3p_d)`.
- The Real-completed carrier dimensions are `(4,6,6)`.
- Type-preserving symmetries cannot permute inequivalent gauge
  representations.
- KMS weights may be tested conditionally, but their energy gaps are not
  treated as inherited.
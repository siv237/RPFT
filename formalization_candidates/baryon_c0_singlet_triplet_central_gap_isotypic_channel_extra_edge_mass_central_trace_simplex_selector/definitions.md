# Definitions

- `central simplex`: positive triples with `p_Y+p_u+p_d=1`.
- `counting convention`: equal central density on each microscopic state.
- `equal-edge convention`: weights chosen so the three quadratic edge
  coefficients coincide.
- `intrinsic selector`: a rule already contained in the parent data and not
  calibrated to an external target.
# Assumptions

- The inherited coherence normalization is `Tr(BB*)=3`.
- The mass scale is normalized to `MM*=I2` for the shape audit only.
- `B` and `M` are initially restricted to the selected rectangular
  subcarrier.
- Six other first-order-compatible complex edge fields exist in the full
  graph and must be restored before a physical closure claim.
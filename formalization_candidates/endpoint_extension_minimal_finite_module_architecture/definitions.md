# Definitions

- The neutral active block is the faithful `M2(C)` module on `s0,a0`.
- The charged family block is the faithful `M3(C)` module completing the
  positive-grading triplet.
- The Real double is forced functorially and is not an independent selector.
# Theorem

> Status: spec-frozen

The minimal endpoint algebra is `M2(C) direct_sum M3(C)`. It has complex
dimension `13`, centre dimension `2`, needs three new independent complex
states, and adds eleven Hermitian directions while preserving `H21` as a
corner. Its physical parent origin remains open.
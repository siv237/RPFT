# Two independent existing scalar modes
> Status: spec-frozen

Prove that `T_B,T_M` have vacuum values `(3,2)` and radial Jacobian
`diag(6,4)`, classify dependent and zero-response invariants, and prove that
the allowed portal space is four-dimensional while the inherited portal is zero.
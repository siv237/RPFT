# Definitions

- `Omega_cfg` is a configuration-source line distinct from endpoints and environment.
- `K_cr=C Omega direct_sum H_n^2 direct_sum H_t^3` is the creation cell.
- The creation frame contains two neutral channels and a full family triplet.
# Классическая плоскость и квантовый строгий минимум
> Status: spec-frozen

Доказать гессиан `diag(0,2a²)` для `a²(c-c0)²` на `c=c0`, а также нулевой
градиент и гессиан `diag(2B,2mu2²)` логарифмического потенциала в
`(a,c)=(mu2,c0)`.
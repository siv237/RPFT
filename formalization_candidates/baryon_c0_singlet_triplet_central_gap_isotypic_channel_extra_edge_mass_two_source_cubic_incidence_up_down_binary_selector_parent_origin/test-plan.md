# Test Plan

1. Verify the exact `U(1)` indices `25/3` and `4/3` and their gap `7`.
2. Verify cancellation of odd moments on particle--conjugate pairs.
3. Verify `Tr(A_ud Y_R^2)=14`.
4. Verify orthogonality, ranks and squared projector distance `12`.
5. Verify `S_ud` eigenvalues `±7/6`, norm `49/3`, and pairing `14`.
6. Check that the result JSON contains no floating-point values.
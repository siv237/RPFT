# Theorem

> Status: spec-frozen

The quadratic hypercharge indices of the two candidates are `25/3` and
`4/3`, while all odd Real hypercharge moments cancel. Hence a positive
externally supplied quadratic score conditionally selects the down-type
candidate. The inherited parent action supplies neither a dynamical binary
incidence variable nor the required typed coupling and its sign, so it does
not derive the binary choice.
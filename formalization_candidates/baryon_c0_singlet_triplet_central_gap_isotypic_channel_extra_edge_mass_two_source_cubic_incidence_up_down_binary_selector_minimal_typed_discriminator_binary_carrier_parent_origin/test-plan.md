# Test Plan

1. Solve the commutant of `diag(5/3,2/3)` exactly.
2. Verify charges `-1,+1` of the off-diagonal transitions.
3. Verify phase covariance of the single-jump dissipator.
4. Verify the common fixed-vector space of the three `so(3)` generators is
   zero.
5. Verify the compensating environment charge is `+1`.
6. Check that all result values are exact and contain no floating-point data.
# Definitions

- `A_ud^cl=C P_u direct_sum C P_d`.
- `Y_ud=diag(5/3,2/3)`.
- `L_down=|d><u|` and `L_up=L_down^*`.
- The environment operator `b_+` has charge `+1`.
- `H_int=g(L_down tensor b_+ + L_up tensor b_+^*)`.
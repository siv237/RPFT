# Assumptions

- The endpoint projectors `P_u,P_d` already occur in the extended carrier.
- The old full noise frame acts inside `End(H_21)`.
- The three old charged-singlet lines transform as an irreducible real
  `SO(3)` triplet.
# Test plan

1. Verify that the predecessor names this gate.
2. Reconstruct the old nine-vertex strict graph.
3. Add `Z_R` and enumerate all new allowed edges.
4. Compute its local anomaly and chiral-index increments exactly.
5. Add an independent `Z_L` and repeat anomaly/index checks.
6. Enumerate all new pair-extension edges and the selected subset.
7. Exhibit two rank-two mass matrices with different kernels.
8. Solve the channel-`SO(3)` mass intertwiner system.
9. Verify the selected `1-6-3` coherence dimensions.
10. Reject every SymPy `Float`.
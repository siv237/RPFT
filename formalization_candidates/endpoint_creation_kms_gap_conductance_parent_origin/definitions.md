# Definitions

- `theta=(theta_s,theta_a,theta_t)` is the dimensionless gap vector.
- `kappa=(kappa_s,kappa_a,kappa_t)` is the conductance vector.
- `B_type` contains multiplicity, grading and charge rows.
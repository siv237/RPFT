# Assumptions

- Endpoint bimodule types are immutable data of the current finite geometry.
- Physical basis changes commute with their central type projectors.
- Group actions are honest actions of `SO(3)` or `A4`, not merely Lie-algebra
  actions or actions of a covering group.
- A conditional extension is not treated as inherited parent data.
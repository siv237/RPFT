# План формализации и проверки

1. Ввести квадратичную функцию `parent` над `Real`.
2. Доказать положительность коэффициента при `phi^2`.
3. Вычислить стационарную точку и проверить производную по `phi`.
4. Нормализовать значение `parent(phiStar,J)` через `field_simp`.
5. Доказать сохранение отношения `couplingSq/massSq` при масштабировании.
6. Доказать статическую нормировку `scaledForm(...,0)=1`.
7. Вычислить производную по `z` в нуле.
8. Проверить свидетели `1/2` и `2/3`.
9. Собрать без `sorry`, новых `axiom` и чисел с плавающей точкой.
# Unique existing composite candidate
> Status: spec-frozen

Show that none of the eight registered carrier classes satisfies the full
literal reuse contract. Show that the only active composite scalar
invariants with the required symmetries are `I_H` and `I_B`, and that only
`I_B` has the already-derived exact nonzero value `3`.

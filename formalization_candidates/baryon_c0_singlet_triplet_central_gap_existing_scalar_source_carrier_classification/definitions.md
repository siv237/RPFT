# Definitions

- `literal_contract`: seven simultaneous requirements for a reusable source carrier.
- `I_H = H^dagger H`: active Higgs norm.
- `I_B = Tr(BB*)`: active edge-coherence radius.
- `active`: belongs to the current inherited carrier without an algebra or endpoint extension.
- `inherited_portal`: a nonzero mixed parent derivative coupling the invariant to `lambda Q`.

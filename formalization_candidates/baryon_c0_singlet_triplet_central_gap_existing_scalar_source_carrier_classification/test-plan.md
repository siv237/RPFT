# Test plan

1. Verify the predecessor names this gate.
2. Exhibit a nonzero infinitesimal gauge action on the Higgs doublet.
3. Verify the centered Higgs norm is bounded below rather than a free Real line.
4. Compute `rank(B*)=1` and `Tr(B*B*^T)=3` exactly.
5. Exhibit nontrivial `SO(3)` action on the vector and traceless-tensor candidates.
6. Check the eight-row literal contract has no passing row.
7. Check exactly two active composite symmetry candidates remain.
8. Reject every SymPy `Float`.

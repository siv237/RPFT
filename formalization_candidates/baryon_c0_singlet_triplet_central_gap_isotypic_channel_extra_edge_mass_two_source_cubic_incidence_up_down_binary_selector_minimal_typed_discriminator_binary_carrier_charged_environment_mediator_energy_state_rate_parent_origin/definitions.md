# Definitions

- `Y_E=diag(0,+1,-1)` and Real conjugation swaps the charged lines.
- `Delta_E=ec-e0` is the essential environment gap.
- The invariant state class is `diag(p0,pc,pc)` with `p0+2pc=1`.
- The physical weak-limit rate is quadratic in the interaction coupling.
# Theorem

> Status: spec-frozen

The doubled-chain carrier of local dimension `48600` simultaneously hosts
all endpoint sectors, the 44-channel collision family, forward transport of
index `45`, and balanced transport of index `1`. The architecture passes
`8/8`; no physical slot is selected.
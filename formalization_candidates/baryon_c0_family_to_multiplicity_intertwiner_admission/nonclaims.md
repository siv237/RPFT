# Что не утверждается

- Семейное действие на multiplicity-среде не считается существующим.
- Endpoint-проекторы не считаются автоматически ковариантным семейством.
- Forgetful-карта из `M3(R)` не считается типизированной.
- `R4+` и одиночная карта `c0` не перенесены физически.
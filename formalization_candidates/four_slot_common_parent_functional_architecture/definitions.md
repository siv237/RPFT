# Definitions

- Endpoint choices are the nested projectors `Q0,Q1,Q2` encoded by `p` in
  the simplex `Delta2`.
- `E_star,chi` are nonnegative continuous coordinates.
- `q` is a parent coordinate whose physical boundary values encode forward
  and balanced transport.
- A physical selector requires an origin for all four coefficient packages.
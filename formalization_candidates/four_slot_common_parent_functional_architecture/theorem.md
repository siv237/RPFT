# Theorem

> Status: spec-frozen

A single bounded polynomial family conditionally selects one endpoint
vertex, positive values of `E_star` and `chi`, and one transport boundary.
The architecture passes `9/9`, while all four selector-coefficient origins
remain open.
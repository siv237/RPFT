# Assumptions

- Full rectangular matrices include every component allowed by their row
  and column carrier, even if a sparse target would set it to zero.
- The three external edge fields are independent complex coordinates.
- Positive stabilizing masses may be studied conditionally but are not
  treated as inherited spectral coefficients.
- The normalized Hessian audit sets all three masses to one.
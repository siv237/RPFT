# Definitions

- `m21=(0,0,2)` and `m24=(1,1,3)` are the multiplicity vectors of the
  affected endpoint representation types.
- `P3(H24)` is the set of Hermitian rank-three projectors on `H24`.
- `C_E=I-2P_E` is a target-loaded typed seed.
# Assumptions

- The inherited spectral action varies `D` on fixed `(A,H,pi,J,Gamma)`.
- No sum or transition measure over finite spectral geometries is inherited.
- System and environment tensor factors remain distinct.